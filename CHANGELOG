
# 0.2.0 (2021-06-08)

- a typo fix, unfortunately it changes the UI a little bit. Listing versions should now be `solc-version versions` instead of `solc-select version`.


# 0.1.0 (2021-06-03)

The initial port of `solc-select`, it tries to be the drop-in replacement for the python version.
